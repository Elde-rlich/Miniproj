<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Journal Dashboard</title>
    <link rel="stylesheet" href="{% static 'authapp/journal-styles.css' %}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{% static 'authapp/home-styles.css' %}">
    <link rel="stylesheet" href="{% static 'authapp/journal-list.css' %}">
</head>
<body>
    <div class="app-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="profile">
                <img src="{% static 'authapp/profile.jpg' %}" alt="profile" class="profile-img">
                <div class="profile-info">
                    <h2>{{ user.username }}</h2>
                    <p>{{ user.email }}</p>
                </div>
            </div>
            <div class="menu">
                <div class="menu-item"><a class="menu-item" href="{% url 'home' %}">Dashboard</a></div>
                <div class="menu-item"><a class="menu-item" href="{% url 'journal_list' %}">Journal</a></div>
                <div class="menu-item"><a class="menu-item" href="{% url 'chatbot' %}">Chatbot</a></div>
                <div class="menu-item"><a class="menu-item" href="{% url 'activity' %}">Activity</a></div>
                <div class="menu-item"><a class="menu-item" href="{% url 'logout' %}">Logout</a></div>
            </div>
        </aside>

        <!-- Main content with curved corners -->
        <div class="main-area">
            <main class="main-content">
                <header class="header">
                    <h1>Journal</h1>
                </header>

                <!-- Black div above the search bar -->
                <div class="journal-header"><!-- You can add an image or style here --></div>

                <!-- Journal list with search bar and create button -->
                <div class="journal-list">
                    <div class="journal-search">
                        <div class="search-bar-container">
                            <i class="bi bi-search search-icon"></i>
                            <input type="text" class="search-bar" placeholder="Search journals">
                        </div>
                    </div>
                    <div class="journal-create">
                        <button class="create-btn">
                            <i class="bi bi-plus"></i>
                            <a href="{% url 'journal_create' %}">
                                <span class="create-text">CREATE</span>
                            </a>
                        </button>
                    </div>

                    <!-- Placeholder for the journal entries fetched from the backend -->
                    <div class="journal-items">
                        {% for journal in journals %}
                        <div class="journal-item">
                            <a href="{% url 'journal_detail' journal.pk %}">
                                <h3>{{ journal.title }}</h3>
                                <p>Created on: {{ journal.date_created|date:"F j, Y" }}</p>
                            </a>
                        </div>
                        {% empty %}
                        <p>No journal entries found.</p>
                        {% endfor %}
                    </div>
                </div>
            </main>
        </div>
    </div>
</body>
</html>
